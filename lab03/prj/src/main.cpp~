#include "../inc/benchmark.h"

int main()
{	
	arr::Kolejka ls;
/*
	ls.insert(1,0);
	ls.insert(2,0);
	ls.insert(3,1);
	ls.insert(4,2);
	ls.insert(5,3);
	
	std::cout << std::endl << ls.erase(0);
	std::cout << std::endl << ls.erase(0);
	std::cout << std::endl << ls.erase(0);
	std::cout << std::endl << ls.erase(0);
	std::cout << std::endl << ls.erase(0);
*/

	ls.push(1);
	ls.push(2);
	ls.push(3);
	ls.push(4);
	ls.push(5);

	std::cout << std::endl << ls.pop();
	std::cout << std::endl << ls.pop();	
	std::cout << std::endl << ls.pop();
	std::cout << std::endl << ls.pop();
	std::cout << std::endl << ls.pop();

//Benchmark();
}
