#include "../inc/benchmark.h"

int main()
{	
	Kontener ls;
	ls.push(0);
	ls.push(1);
	ls.push(2);
	ls.push(3);
	ls.show();

	ls.insert(4,1);
	ls.show();
	ls.insert(5, 2);
	ls.show();
	ls.insert(6,3);
	ls.show();	
	 //Benchmark();
}
